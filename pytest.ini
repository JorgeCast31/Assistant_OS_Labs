[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -q --ignore=scratch
filterwarnings =
    ignore::DeprecationWarning
markers =
    integration: marks tests as integration tests (require external services, skipped by default)
