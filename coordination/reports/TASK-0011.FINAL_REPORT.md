---
id: TASK-0011-design-command-center-control-plane-v0
agent: claude
authority: proposed
status_at_report: EVIDENCE_READY
created_at: 2026-06-23
---

> **Nota de corrección (2026-06-23, rev. coherencia).** Esto **no** es un fallo de éxito fabricado ni
> retracta el diseño; corrige una afirmación de estado del PR y añade una distinción arquitectónica.
> Verdad operativa vigente: **PR #259 está abierto (`isDraft: false`, `mergeable`), listo EXCLUSIVAMENTE
> para revisión de Codex; NO autorizado para merge ni implementación.** Un PR abierto/mergeable es
> **evidencia, no autoridad**. Cierres previos que dijeron "Draft PR / isDraft: true" quedan corregidos
> por esta nota (ver §2 H4). Además se añadió la distinción de **tres planos** (Authority / Delivery-
> attention / Evidence) y el requisito de UI "no live agent channel". El análisis previo se preserva
> íntegro abajo. Estado: `READY_FOR_CODEX_REVIEW` (sin cambio de `status`/`authority`).

## 1. Estado del sistema

- **Baseline:** `origin/main` @ `b0fbb5a` (merge de PR #258; TASK-0010 ya en main). Worktree limpio
  alineado a ese SHA; rama de trabajo `coordination/task-0011`.
- **Qué funciona / está confirmado (read-only):**
  - Mission Control existe (`ui/components/sovereign/MissionControlView.tsx`) con vistas Planner / MSO /
    Arms / Orchestration / Outcome.
  - `execution_allowed` y `can_execute_now` invariantemente `False`
    (`assistant_os/mso/authority_binding.py:102-103`, `authority_preparation.py:205`).
  - Police gate antes de dispatch en el orchestrator.
  - Read-Only Queue Reporter manual/read-only/sin autoridad
    (`scripts/coordination_queue_reporter.py`); Runner bloqueado.
  - Contrato `coordination/` v3.1 (AGENT_CONTRACT / RULES_OF_ENGAGEMENT / schemas) vigente.
- **Qué NO se habilitó (por diseño):** ejecución, Runner, provider integration, headless/cron/Actions,
  escritura de estado canónico desde la UI. Nada de eso se tocó ni se autorizó.

## 2. Hallazgos

- **H1 (baseline desplazado):** durante `git fetch`, `origin/main` avanzó de `f26863c` a `b0fbb5a`
  (PR #258 ya mergeado). Decisión: usar `b0fbb5a` como baseline real (la tarea pide trabajar desde
  `origin/main`) y tratar #258 solo como historial — no se reabre, no se modifica, no se usa como
  autorización. Consecuencia: el siguiente ID válido es **TASK-0011** (TASK-0010 ya existe por #258).
- **H2 (deuda preexistente, no de esta tarea):** STATE.md/TASK-0007 ya documentan la reconciliación de
  la autorización consumida (TASK-0010). No es objeto de esta tarea; no se modifica.
- **H3 (riesgo de interpretación):** el principal riesgo del entregable es que el diseño se lea como
  autorización de implementación. Mitigado explícitamente: el backlog y la TASK declaran que **no
  autorizan** implementación; cada incremento requiere su propio `READY` en `main` por Jorge.
- **H4 (coherencia de estado del PR):** el cierre anterior afirmó "Draft PR / isDraft: true", pero
  GitHub reporta PR #259 como `isDraft: false`, `state: OPEN`, `mergeable` (`gh pr create --draft` no
  quedó como Draft a nivel de GitHub). Corrección aplicada **sin** convertir el PR a Draft y **sin**
  mergear: la verdad operativa es "PR abierto, listo solo para revisión de Codex; no autorizado para
  merge ni implementación". Refuerza la distinción de **tres planos** (§9.1 de la arquitectura): un PR
  abierto/mergeable es **evidencia**, nunca **autoridad**; solo el merge verificable de Jorge confiere
  `human_final`.
- **H5 (corrección arquitectónica incorporada):** se añadió la distinción explícita de tres planos
  (Authority / Delivery-attention / Evidence), el requisito de UI v0 ("Model delivery occurs outside the
  authority plane. No live agent channel is configured."; las entregas son no autoritativas y no alteran
  `TASK.status`), y el contrato de dispatch no autoritativo en `Increment 5` del backlog.

## 3. Cambios realizados

Todos en rama `coordination/task-0011`, nunca en `main`. Seis archivos:

| Archivo | Motivo | Impacto |
|---|---|---|
| `docs/architecture/COMMAND_CENTER_CONTROL_PLANE_V0.md` | Propuesta arquitectónica (problema, autoridad, actores, componentes reutilizables/faltantes, modelo de datos conceptual, máquina de estados compatible, flujo, límites, contrato inter-modelo, amenazas, out-of-scope, decisiones de Jorge). | Documental. Define contrato de proyección compatible con coordination/. No ejecuta nada. |
| `docs/architecture/COMMAND_CENTER_UI_V0_SPEC.md` | Especificación de UI (info en Mission Control, lentes, mission card, estado por actor, indicadores que nunca afirman ejecución, acciones permitidas/no permitidas, estados vacíos/error, criterios de aceptación AC-V1..V10). | Documental. UI = proyección; sin control agente-accionable de execute/approve/merge. |
| `docs/roadmaps/COMMAND_CENTER_V0_IMPLEMENTATION_BACKLOG.md` | Backlog de 6 incrementos pequeños/reversibles con objetivo/scope/no-scope/módulos/dependencias/DoD/tests/riesgo/stop/Codex/decisión humana. | Documental. Declara que no autoriza implementación. |
| `coordination/tasks/TASK-0011.md` | Tarea canónica (DRAFT, author/assigned claude, reviewer codex) que autoriza solo el diseño documental. | Propuesta de estado en rama; READY solo lo fija Jorge en main. |
| `coordination/worklogs/TASK-0011.WORKLOG.md` | Bitácora append-only de la ejecución. | Trazabilidad. |
| `coordination/reports/TASK-0011.FINAL_REPORT.md` | Este reporte. | Evidencia de cierre del tramo de ejecutor. |

## 4. Validaciones

- **V1 — baseline:** `git rev-parse origin/main` = `b0fbb5a`; worktree alineado y limpio. **OK.**
- **V2 — scope del diff:** se verificará con `git status`/`git diff --stat` que solo aparecen los seis
  archivos del scope. (Ver sección de cierre del WORKLOG; el diff debe contener exactamente esos seis.)
- **V3 — verdades de partida:** confirmadas por inspección read-only (grep `can_execute_now|
  execution_allowed` → siempre `False`; Mission Control y Reporter presentes). **OK.**
- **V4 — sin ejecución:** no se corrió ningún script de dominio, ni se tocó MSO/Police/Policy/auth/
  workflows/.env/secretos. Esta tarea es documental; no hay tests de código que correr (no se añadió
  código). **N/A por diseño**, declarado explícitamente (sin éxito fabricado).
- **V5 — contrato:** TASK/WORKLOG/FINAL_REPORT siguen `schemas/*` (campos required presentes; `authority:
  proposed`; `status: DRAFT`; `forbidden` incluye mso/police/policy/auth/.env/secrets/workflows). **OK.**

> Nota de veracidad operativa: esta tarea **no ejecuta** y no afirma ninguna validación de runtime. No
> hay suite de tests asociada porque no se introdujo código; declararlo es parte del contrato, no una
> omisión.

## 5. Riesgos residuales

- **R1:** El diseño podría leerse como permiso de implementación. Mitigado en texto (backlog + TASK), pero
  la decisión de avanzar a cualquier incremento es de Jorge (READY separado).
- **R2:** El modelo de prioridad de Mission y el enlace Mission↔Work Package podrían requerir un cambio de
  contrato (`TASK.schema.md`). Queda como **decisión abierta para Jorge** (no resuelta aquí, fail-closed).
- **R3:** Increment 4 (safe-prepare) es la superficie más cercana a ejecución; el diseño la restringe a
  "proposal artifact only", pero su implementación futura exigirá escrutinio alto de Codex.

## 6. Decisión propuesta

**NEEDS_CHANGES → no.** Propongo **GO documental** (`authority: proposed`) para que Codex revise la
propuesta. Esta decisión es **propuesta**, no final: no fija `READY`, no avanza a `HUMAN_DECISION`, no
autoriza implementación ni ejecución. El estado efectivo solo lo materializa Jorge en `main`.

Estado del tramo de ejecutor: **READY_FOR_CODEX_REVIEW**.
