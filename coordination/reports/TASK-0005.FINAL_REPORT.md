---
id: TASK-0005-runner-manual-dry-run
agent: claude
authority: proposed
status_at_report: DRAFT
created_at: 2026-06-17
---

> **BANNER — Simulación, no ejecución.** Este reporte documenta un **dry-run manual** del protocolo del Runner, realizado por **Claude leyendo archivos de `main`**. **No** hubo Runner, **no** hubo código, **no** se mutó ningún estado en `main`. `TASK-0005` permanece en `DRAFT`: este reporte **no** la avanza a `EVIDENCE_READY` ni habilita ningún tramo. `status_at_report: DRAFT` es deliberado (avanzar sin `READY` in-file de Jorge repetiría la brecha de TASK-0001). El Runner sigue **bloqueado**.

## 1. Estado del sistema

- **Bus `coordination/` en `main`:** contrato v3.1 vigente; 4 tareas (`TASK-0001..0004`).
- **Lo que funciona:** el protocolo manual del Runner (`RUNNER_MANUAL_PROTOCOL_DRAFT.md`) es **operable a mano**: con solo el front-matter de cada `TASK.md` y la presencia de artefactos en `main`, se puede clasificar cada tarea y construir una cola sin ejecutar nada y sin ninguna capacidad de autoridad.
- **Lo que no funciona / no aplica:** no hay Runner implementado (correcto: sigue bloqueado). El protocolo, tal como está, **no distingue** algunos casos de borde (ver Hallazgos F1–F4).

## 2. Hallazgos

**Cola de trabajo manual (derivada de `main`):**

| Task | status | acción legal | rol | clasificación |
|---|---|---|---|---|
| TASK-0001 | DRAFT | ninguna automática | Jorge/observar | AMBIGUA / BLOCKED-OBS (falta `last_legit_status`; dogfood fallido preservado) |
| TASK-0002 | HUMAN_DECISION | `→ HANDOFF_TO_MSO` (MSO, fuera del plano) | MSO / no-op | NO-OP (cerrada en el plano) |
| TASK-0003 | DRAFT | `DRAFT → READY` (Jorge) | Jorge | DRAFT SUPERSEDIDA (v3 ya activo) |
| TASK-0004 | DRAFT | `DRAFT → READY` (Jorge) | Jorge | DRAFT con diseño mergeado (próximo real = dry-run) |

**Fricciones / ambigüedades (el valor real del dry-run):**

- **F1 — TASK-0001 sin `last_legit_status` (campo `required` v3).** Fail-closed estricto la marcaría BLOCKED-OBS para siempre, pero es un **dogfood fallido preservado a propósito** (naming v1). El diseño no contempla tareas **históricas/archivadas/exentas**.
- **F2 — DRAFT supersedido (TASK-0003).** `status: DRAFT` pero su contenido **ya es contrato activo** (#246/#247/#249). El mapeo ingenuo `DRAFT → "Jorge fija READY"` es **engañoso**. No existe un estado para "entregable adoptado, tarea quedó en DRAFT".
- **F3 — Enum insuficiente para "siguiente paso" (TASK-0004).** El próximo paso real (dry-run) vive en prosa (`next_action`), que el diseño manda **ignorar** como instrucción (§R5). Tensión: enum-solo vs. no-leer-texto-libre.
- **F4 — `HUMAN_DECISION` terminal-en-plano vs. enum.** El Runner hace **no-op** (correcto), pero el enum sugiere `HANDOFF_TO_MSO` como "siguiente"; debería etiquetarse "a la espera de MSO (fuera del plano)", no accionable.

## 3. Cambios realizados

Todo en rama `coordination/task-0005-runner-manual-dry-run` (nunca `main`):

- `coordination/tasks/TASK-0005.md` — **nuevo**, `status: DRAFT`. Motivo: portador canónico de esta tarea de simulación. Impacto: ninguno en `main` hasta merge de Jorge.
- `coordination/worklogs/TASK-0005.WORKLOG.md` — **nuevo**, append-only. Motivo: bitácora del dry-run (fuentes, pasos, tabla, fricciones). Impacto: evidencia auditable.
- `coordination/reports/TASK-0005.FINAL_REPORT.md` — **nuevo** (este archivo). Motivo: conclusiones. Impacto: habilita revisión de Codex.
- `coordination/proposals/RUNNER_DRY_RUN_OBSERVATIONS.md` — **nuevo**, no autoritativo. Motivo: ajustes propuestos al diseño antes del MVP. Impacto: insumo para TASK-0006.

**No** se tocó: MSO/Police/Policy/Auth, `.github/workflows`, secrets, `.env`, `STATE.md`, `TASK-0002.md`, `TASK-0004.md`, `TASK-0002.DECISION.md`, `TASK-0002.DECISION_CANDIDATE.md`, `cowork/`. **No** se movió ninguna otra tarea. **No** se fijó `HANDOFF_TO_MSO`.

## 4. Validaciones

- **¿Dry-run posible sin código?** **Sí.** Verificado: clasificación completa de 4 tareas usando solo lectura de `main`.
- **¿Cola útil?** **Sí.** Distingue accionable (Jorge) vs. no-op (MSO/cerrada) vs. ambigua. Reveló 2 DRAFT engañosas (0003, 0004) y 1 ambigua (0001) que un humano podría pasar por alto.
- **¿No-autoridad?** **Confirmado.** Cero merge/approve/promote/execute. El protocolo no necesitó autoridad. (`RUNNER_DESIGN_REVIEW.md §7` se sostiene.)
- **¿Detecciones requeridas (misión §6)?** TASK-0002 cerrada en `HUMAN_DECISION` ✓; TASK-0004 DRAFT/diseño mergeado ✓; Runner bloqueado ✓; próximo paso = dry-run/manual review, no MVP ✓.
- **Validaciones no corridas:** ninguna prueba automatizada (no aplica; no hay código). Declarado explícitamente para no fabricar éxito.

## 5. Riesgos residuales

- El diseño del Runner **no resuelve aún** F1–F4. Si se implementara un MVP sin abordarlas, produciría ruido (TASK-0001 perma-bloqueada) o notificaciones engañosas (DRAFT supersedidas). **Mitigación:** F1–F4 entran como **restricciones de entrada** a TASK-0006.
- Tensión de diseño F3 (enum-solo vs. ignorar prosa) requiere una decisión explícita en el MVP: o se añade un estado/etiqueta machine-readable, o el MVP solo **reporta y marca**, sin proponer transición concreta para DRAFTs ambiguas.
- Este reporte **no** cierra TASK-0005 ni la avanza; depende de la revisión de Codex y de Jorge.

## 6. Decisión propuesta

**GO** (authority=proposed) — **pasar a `TASK-0006 — MVP Read-Only Queue Reporter Proposal`**, con la condición de que F1–F4 se traten como insumos obligatorios de su diseño.

Justificación: el dry-run manual fue posible sin código, la cola es útil y el protocolo no requiere autoridad. Las ambigüedades halladas son **menores y resolubles**, y precisamente confirman el valor de un MVP **read-only que reporta y marca** (no que decide). El MVP propuesto es seguro porque no adquiere autoridad; debe limitarse a (a) marcar tareas históricas/exentas, (b) señalar DRAFTs supersedidas en vez de proponer `READY` a ciegas, (c) etiquetar `HUMAN_DECISION` como "a la espera de MSO (fuera del plano)".

> Este GO es una **propuesta**, no autoridad. Solo Jorge decide, por evento verificable, si se abre TASK-0006. El Runner permanece bloqueado.
