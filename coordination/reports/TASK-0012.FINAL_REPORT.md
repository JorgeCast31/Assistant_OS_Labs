---
id: TASK-0012-aos-p0-reality-lock
agent: claude
authority: proposed
status_at_report: EVIDENCE_READY
created_at: 2026-06-24
---

## 1. Estado del sistema

**Cleanroom:** `C:\Users\Jorge\AOS_P0_Cleanroom` — clean clone of canonical main  
**Baseline SHA:** `b2fa39b9ef1dd7dbee7a1c7fa4603ec42f57675d`  
**Branch:** `audit/aos-p0-reality-lock`  
**Quarantined checkout:** `C:\Users\Jorge\Assistant_OS_Labs` — untouched, not inspected

### What works (canonical main)

- Police `check()` enforcement gate: V1–V5 validations functional and tested.
- `policy_decision_ref = "auto:<intent_id>"`: emitted by webhook_server N0 path, passes Police V3 structural check.
- `approval:auto:<intent_id>`: emitted by machine_operator_adapter N0 compat path, used as synthetic approval_id.
- `PlaywrightRuntimeDispatcher.execute()`: importable and callable; sovereign gate at HTTP server layer is the upstream control.
- Policy/governance/police roles are distinct and well-separated in canonical main.

### What does not exist in canonical main

- `assistant_os/mso/governed_execution.py` — does NOT exist. Any reference to this file in prior reports or the quarantined checkout is historical or local-only state.

---

## 2. Hallazgos

### Finding A — Police `check()` is a token-bound enforcement gate (VERIFIED)

- **File:** `assistant_os/police/enforcement.py:85–264`
- Police validates token lifecycle (V1–V1.7), structural presence of governance_ref (V2) and policy_decision_ref (V3), binding consistency (V4–V4.6), and capability scope (V5).
- V3 is structural-only: `if not request.policy_decision_ref` — presence check. Does NOT call `evaluate_policy()` or `evaluate_governance()`.
- Authority chain roles are distinct: policy producer (`policy/policy_engine.py`), governance producer (`mso/governance_engine.py`), enforcement gate (`police/enforcement.py`), context/enrichment layer (`identity_guard.py`, `cognition/`), legacy path (`policy_engine.py` root level).
- **Proven by test:** `TestPolicePolicyDecisionRefIsStructuralOnly::test_v3_does_not_call_evaluate_policy`

### Finding B.1 — `policy_decision_ref = "auto:<intent_id>"` family (VERIFIED)

- **Emitted at:** `webhook_server.py:6448`
- **Validation:** Police V3 presence-only; OpenClaw server format regex only. No semantic provenance verification.
- **Can reach execution:** YES (N0 read-only flow → sovereign gate).
- **Unverified claim to reject:** Police does NOT reject `"auto:..."` values. V3 accepts any non-empty string.
- **Proven by test:** `TestPolicePolicyDecisionRefIsStructuralOnly::test_v3_accepts_auto_prefixed_ref`

### Finding B.2 — `approval:auto:<intent_id>` family (VERIFIED)

- **Emitted at:** `mso/machine_operator_adapter.py:1852`
- **Role:** synthetic `approval_id` in N0 compatibility artifact (not a Police token_ref).
- **Distinctness:** confirmed distinct from `policy_decision_ref = "auto:<intent_id>"`.
- **Proven by tests:** `TestMachineOperatorAdapterN0SyntheticApproval`

### Finding C — OpenClaw runtime boundary (VERIFIED, external bypass UNPROVEN)

- **`PlaywrightRuntimeDispatcher.execute()`:** importable, callable.
- **No local sovereign gate:** confirmed — `execute()` validates capability_name and URL only.
- **Production call-site:** `openclaw_backend/server.py:492`.
- **Gate before call-site:** `_sovereign_store.is_execution_allowed()` at `server.py:441` (sovereign gate; denied → HTTP 403).
- **External exploitable bypass:** UNPROVEN — not established from code alone; depends on network binding and auth bypass, not proved.
- **In-process access concern:** noted — direct Python call to `execute()` skips server-level sovereign gate. Threat model question for Jorge.

### Finding D — Canonical vs non-canonical evidence (VERIFIED)

- Current canonical: GitHub main at SHA `b2fa39b9`.
- Quarantined checkout: not inspected, not used as evidence.
- Historical audits: reflect past state, not current canonical.
- All findings above are from cleanroom observations of canonical main only.

---

## 3. Cambios realizados

| File | Reason | Impact |
|---|---|---|
| `docs/audits/AOS_P0_REALITY_LOCK.md` | Main audit document with findings A–D | Documentary only |
| `docs/audits/AOS_P0_AUTHORITY_SURFACE_MAP.md` | Authority surface component map | Documentary only |
| `tests/test_aos_p0_characterization.py` | Characterization tests proving findings | 8 tests, non-productive |
| `coordination/tasks/TASK-0012.md` | Coordination task record | Coordination only |
| `coordination/worklogs/TASK-0012.WORKLOG.md` | Execution worklog | Coordination only |
| `coordination/reports/TASK-0012.FINAL_REPORT.md` | This report | Coordination only |

**No production code modified.** No mso/, police/, policy/, auth/, ui/, .github/, .env touched.

---

## 4. Validaciones

### Test run — AOS-P0 characterization tests

**Command:** `python -m pytest tests/test_aos_p0_characterization.py -v`  
**Environment:** Python 3.11.9, pytest 9.0.1, platform win32  
**Cleanroom path:** `C:\Users\Jorge\AOS_P0_Cleanroom`

**Raw output:**
```
============================= test session starts =============================
platform win32 -- Python 3.11.9, pytest-9.0.1, pluggy-1.6.0
rootdir: C:\Users\Jorge\AOS_P0_Cleanroom
configfile: pytest.ini
collected 8 items

tests\test_aos_p0_characterization.py ........                           [100%]

============================== 8 passed in 0.63s ==============================
```

**Collected:** 8  
**Passed:** 8  
**Failed:** 0  
**Skipped:** 0

### Tests NOT run

The full test suite was NOT run in this task. Full suite validation is a separate
concern and out of scope for AOS-P0 characterization. The 8 characterization tests
are narrowly scoped to prove findings A–C.

---

## 5. Riesgos residuales

1. **N0 synthetic ref provenance gap:** `policy_decision_ref = "auto:<intent_id>"` is Police-accepted but semantically unverified. No actual `evaluate_policy()` was called to produce it. Risk: authority envelope with no real policy evaluation on the critical path for N0 reads.

2. **`approval:auto:` compatibility artifact:** synthetic approval envelope with no prior authorization event. Intended design for N0, but not explicitly documented as such in the canonical design docs.

3. **OpenClaw in-process access:** `PlaywrightRuntimeDispatcher.execute()` has no internal sovereign gate. An actor with direct Python process access could call it without the server's sovereign gate. Whether this is in-scope for the threat model is unresolved.

4. **OpenClaw network reachability:** Unknown from code alone whether HTTP server is externally reachable. If it is, auth gate sufficiency is unconfirmed.

5. **`governed_execution.py` absence:** This file does not exist in canonical main but was apparently referenced in prior audits. The gap between local checkout and canonical main is unknown (quarantine boundary prevents inspection). What was in that file and whether its absence is intentional or a merge gap is unresolved.

---

## 6. Decisión propuesta

**GO (proposed, authority=proposed)**

Rationale: The cleanroom package accurately characterizes the authority surface as it exists in canonical main. All four required findings (A–D) are documented with exact file:line references. The 8 characterization tests pass and prove the key structural properties. No production code was modified. The quarantined checkout was untouched.

This PR authorizes:
- Merging the documentary and characterization package into main.

This PR does NOT authorize:
- Any runtime change
- Any execution or Runner activation
- Any provider integration or ModelBridge change
- Any policy, Police, or authority change
- Any change to authentication, .env, secrets, or .github/

Four open decisions remain for Jorge (documented in `AOS_P0_REALITY_LOCK.md` § "Decisions Required").
