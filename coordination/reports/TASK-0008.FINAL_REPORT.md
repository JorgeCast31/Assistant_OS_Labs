---
id: TASK-0008-implement-read-only-queue-reporter
agent: claude
authority: proposed
status_at_report: DRAFT
created_at: 2026-06-18
---

> **Nota de naturaleza.** Este reporte documenta la **implementación** del MVP Read-Only Queue Reporter en rama `coordination/task-0008-implement-read-only-queue-reporter`. **No** afirma haber tocado `main`, ni MSO/Police/Policy/Auth. La decisión propuesta es **propuesta** (`authority=proposed`); no avanza por sí sola. Siguiente paso: revisión de Codex. **No mergear por agente.**

## 1. Estado del sistema

- **Precondición cumplida:** PR #255 (autorización de TASK-0007) está **MERGEADO** en `main` por Jorge (`JorgeCast31`, 2026-06-17T22:37:59Z). La implementación quedó autorizada por evento verificable.
- **Entregado y funcionando:** `scripts/coordination_queue_reporter.py` — lector manual read-only que lee `coordination/tasks/*.md`, extrae front-matter, detecta presencia de artefactos, clasifica (F1–F4) e imprime una cola a stdout. CLI con `--help` y root opcional.
- **Tests:** `tests/test_coordination_queue_reporter.py` — 25 casos, **25 passed**.
- **No tocado:** `assistant_os/`, `ui/`, `auth/`, MSO/Police/Policy, `.github/workflows/`, `coordination/STATE.md`, TASK-0002/0004/0005/0006/0007 y sus decisiones/candidatos.

## 2. Hallazgos

- `requirements.txt` no incluye dependencia YAML ⇒ se implementó un **parser de front-matter de stdlib** (cumple S2 anti-inyección; sin `eval`/deserializadores inseguros).
- El enum de `status` y los campos `required` viven como **prosa Markdown** en `schemas/TASK.schema.md`; parsearlos en runtime introduciría no-determinismo. Se embebieron como **constantes** (mirror documentado del schema v3) para garantizar S5 (determinismo).
- La distinción `DRAFT_SUPERSEDED` vs `DRAFT_DESIGN_MERGED` **no es decidible** por el MVP (lo dice la propia spec). El reporter emite `DRAFT_SUPERSEDED` + `REQUIRES_HUMAN_INTERPRETATION` y deja ambas etiquetas candidatas en `notes`; decide Jorge.
- En el repo, `assistant_os` ya está cargado por `conftest` durante la sesión de pytest; el test de runtime S7 se ajustó para medir el **delta** de `sys.modules` (que el reporter no introduzca `assistant_os`), no su ausencia global.

## 3. Cambios realizados (rama coordination/task-0008-implement-read-only-queue-reporter)

| Archivo | Motivo | Impacto |
|---|---|---|
| `scripts/coordination_queue_reporter.py` | Implementar el MVP (lectura + clasificación + reporte) | Nuevo script manual read-only; sin autoridad; aislado de `assistant_os/` |
| `tests/test_coordination_queue_reporter.py` | Contrato de prueba (F1–F4, S1–S7, determinismo, frontmatter inválido) | Nueva suite; 25 passed |
| `coordination/tasks/TASK-0008.md` | TASK canónica de esta implementación | `status: DRAFT`, `authority: proposed` |
| `coordination/worklogs/TASK-0008.WORKLOG.md` | Bitácora append-only | Trazabilidad de la implementación |
| `coordination/reports/TASK-0008.FINAL_REPORT.md` | Este reporte | Cierre de evidencia (propuesto) |

## 4. Validaciones

- `pytest tests/test_coordination_queue_reporter.py -q` ⇒ **25 passed in 0.21s** (real, ejecutado).
- Corrida manual `python scripts/coordination_queue_reporter.py` ⇒ cola coherente con `MVP §10` y el dry-run de TASK-0005:
  - TASK-0001 ⇒ `LEGACY_AMBIGUOUS` (F1; degrada solo esa fila).
  - TASK-0002 ⇒ `CLOSED_IN_COORDINATION_PLANE` + `MSO_ONLY_NEXT` (F4; sin empujar HANDOFF a MSO).
  - TASK-0003/0004/0005/0006 ⇒ `DRAFT_SUPERSEDED` + `REQUIRES_HUMAN_INTERPRETATION` (F2; nunca READY ciego).
  - TASK-0007 ⇒ `READY_FOR_EXECUTOR` (READY en `main` tras #255).
- Invariantes de seguridad cubiertas: S1 (sin apertura en escritura), S2 (anti-inyección: prosa imperativa inerte), S3 (sin red/secretos en superficie), S4 (sin persistencia: mtimes idénticos antes/después), S5 (determinismo), S6 (fail-closed), S7 (sin `import assistant_os`).

## 5. Riesgos residuales

- El parser de front-matter cubre el subconjunto YAML usado por las TASK reales; YAML exótico (anclas, multilínea `|`/`>`, flow maps) no se soporta — por diseño cae a fail-closed (`BLOCKED_OBS`), nunca a una acción errónea.
- El enum/required embebidos deben mantenerse sincronizados si `TASK.schema.md` cambia (documentado en el código). Drift de schema ⇒ riesgo de clasificación obsoleta, mitigado por fail-closed.
- La detección de `deliverables_present` (F2) compara solo **presencia** de paths declarados; no valida contenido (consistente con el MVP: ante duda, marca, no decide).

## 6. Decisión propuesta

**GO (propuesto, `authority=proposed`)** para pasar a **revisión de Codex** contra los criterios de aceptación (`MVP §13` + `ACCEPTANCE_TESTS` T-C*/T-F*/T-S*). La implementación respeta el scope-lock de TASK-0007: read-only, sin autoridad, sin escritura, sin red, sin tocar la soberanía. **No** es `READY` ni autoriza ejecución; el Reporter/Runner sigue bloqueado como actor con autoridad hasta decisión humana verificable de Jorge.
